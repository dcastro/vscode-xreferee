{
  "name": "xreferee",
  "displayName": "xreferee",
  "publisher": "dcastro",
  "description": "Validate cross references throughout a git repo.",
  "version": "0.0.3",
  "repository": {
    "type": "git",
    "url": "https://github.com/dcastro/vscode-xreferee.git"
  },
  "bugs": {
    "url": "https://github.com/dcastro/vscode-xreferee/issues"
  },
  "engines": {
    "vscode": "^1.109.0"
  },
  "categories": [
    "Linters"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "xreferee",
      "properties": {
        "xreferee.serverArgs": {
          "type": "string",
          "default": "",
          "description": "Additional command-line arguments passed to the xreferee language server process (for example: --log-file <file>).",
          "scope": "resource"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "26.x",
    "@types/vscode": "^1.109.0",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0",
    "esbuild": "^0.28.1",
    "eslint": "^10.6.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.0"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.1",
    "extract-zip": "^2.0.1",
    "string-argv": "^0.3.2",
    "tar": "^7.5.17",
    "vscode-languageclient": "^10.0.1"
  }
}
